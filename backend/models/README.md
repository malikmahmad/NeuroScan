# Model Weights

Place trained `.pth` checkpoint files here before starting the server:

```
models/
├── cnn_best.pth
├── efficientnet_best.pth
├── vit_best.pth
└── unet_best.pth
```

These are not committed to git (see `.gitignore`).

Train using the notebooks in `../notebooks/` on Kaggle GPU T4.
